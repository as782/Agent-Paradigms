[project]
name = "agent-paradigms"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-search-results>=2.4.2",
    "openai>=2.24.0",
    "python-dotenv>=1.2.2",
]
